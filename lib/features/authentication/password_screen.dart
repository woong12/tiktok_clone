import 'package:flutter/material.dart';

class PassWordSceen extends StatelessWidget {
  const PassWordSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
